import React from 'react';

const Home = () =>{
    return (
        <div>
            <div>Home page save </div>
            <button type="button" onClick = {() => console.log("inside button clicked")}>click me </button>
        </div>
    
    );
}

export default Home;